# Not yet installed/run (report.smk is not yet implemented, see
# workflow/rules/report.smk) - these versions are recommended pins, not
# versions validated by an actual run. Update to the versions actually
# resolved on first install.
name: multiref-reporting
channels:
  - conda-forge
dependencies:
  - python=3.11.11
  - pandas=2.2.3
  - numpy=2.2.3
  - matplotlib=3.10.0
  - jinja2=3.1.5
