# Phase I (Section 6.2): einheitliche Genannotation ueber alle 14
# Referenzen (BRAKER3/Liftoff + InterProScan/eggNOG-mapper + OrthoFinder).
#
# Status: NICHT implementiert. Blockiert auf envs/annotation.yaml
# (BRAKER3 braucht zusaetzlich eine separat zu beschaffende
# GeneMark-ES/ET-Lizenzdatei, siehe
# https://github.com/Gaius-Augustus/BRAKER - kein reiner conda-Install).
