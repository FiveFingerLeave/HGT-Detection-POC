# Phase VI+VII (Section 11-12): fensterbasierte PAV-Analyse (mosdepth auf
# 10-kb-/2-kb-Fenstern, Present/Absent/Ambiguous-Regeln je Regionstyp)
# und Long-Read-SV-Calling (Sniffles2), kombiniert zur finalen
# PAV-Evidenz je Kandidatenregion (candidate_region_calls.tsv).
#
# Status: NICHT implementiert. Voraussetzung: Mapping-BAMs aus
# mapping.smk und Kandidatenregionen aus panel.smk/starships.smk.
