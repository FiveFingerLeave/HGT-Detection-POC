# Phase IX (Section 14): Referenz-Rarefaction (Panel-Saettigung ueber
# zufaellige k-aus-14-Referenzkombinationen, 1000 Permutationen) und
# Testisolat-Rarefaction/Novelty-Check (unmapped Reads -> lokale
# Assemblies -> panel-externe Kandidatenregionen).
#
# Status: NICHT implementiert. Voraussetzung: vollstaendiger
# Kandidatenkatalog (panel_contig_manifest.tsv) aus panel.smk fuer alle
# 14 Referenzen.
