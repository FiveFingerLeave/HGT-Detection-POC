# Phase I (Section 6.3): RepeatModeler2 + RepeatMasker (oder EDTA) auf
# allen 14 Referenzen; Repeat-Anteil pro Contig/Analysefenster.
#
# Status: NICHT implementiert. Braucht envs/annotation.yaml
# (repeatmodeler/repeatmasker) - Bibliothekserstellung pro Genom ist
# rechenintensiv (Stunden pro Genom), nach der Referenz-QC (qc.smk)
# einzuplanen, nicht parallel dazu auf derselben Maschine.
